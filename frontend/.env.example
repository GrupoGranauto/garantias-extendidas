# Solo variables con prefijo VITE_ llegan al browser.
# NUNCA poner aqui service_role key ni la password de la BDD: quedan publicas en el bundle.
VITE_API_URL=http://localhost:4000

# Opcional: cliente Supabase en browser (solo anon key, protegida por RLS)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
